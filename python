Olá, hoje eu venho trazer uma script para trabalharem no Visual Studio Code para Bot Discord. Com os comandos Ban e Kick;
Para comecarmos estarei colocando o arquivo principal e logo em seguida os comandos;

>> main.py 

import discord
from discord.ext import commands

bot = commands.Bot("PREFIXO")


@bot.event
async def on_ready():
    print(f"MENSAGEM PARA EMITIR NO TERMINAL QUANDO FICAR ON")


>> Comando de BAN

@client.command() # Define um comando
async def ban(ctx, membro: discord.Member, *,motivo=None):
  canal = client.get_channel(id=) # Canal de Logs
  msg = f'{ctx.author.mention} **Baniu** {membro.mention} **Motivo: _{motivo}_**'
  await membro.ban()
  await canal.send(msg)

>> Comando de Kick

@client.command() # Define um comando
async def kick(ctx, membro: discord.Member, *,motivo=None):
  canal = client.get_channel(id=) # Canal de Logs
  msg = '{ctx.author.mention} **Kickou** {membro.mention} **Motivo: {motivo}**'
  msg = f'O {membro.mention} foi kickado com sucesso.'
  await membro.kick()
  await canal.send(msg)
